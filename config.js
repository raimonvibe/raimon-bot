module.exports = {
    prefix: "!", // Example prefix, not used with slash commands but could be added
    defaultCooldown: 3, // seconds for command cooldowns (if implementing)
    // More complex configurations could be added here later
  };